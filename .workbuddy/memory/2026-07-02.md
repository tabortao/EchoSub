# 2026-07-02 工作日志

## EchoSub 学习页面融入专辑 + 首页优化（4 项需求）

完成用户提出的 4 项前端重构需求，后端零改动。

### 改动文件
- `frontend/src/index.css` — 移除 `#root` 的 `width:1126px/margin:auto/text-align:center/border-inline`，内容铺满浏览器
- `frontend/src/utils/index.ts` — 新增 `markdownToPlainText(md)` 工具函数（块级→行内顺序处理 Markdown 符号）
- `frontend/src/layouts/MainLayout.tsx` — 移除「学习页面」导航项 + `ReadOutlined` import
- `frontend/src/router/index.tsx` — 新增 `/notes/:id` 路由（NoteEditorPage）
- `frontend/src/pages/NoteEditor.tsx` — **新建**，从 StudyNotes 剥离的编辑器独立页面，`useParams` 取 id，`navigate(-1)` 返回（无历史栈兜底 `/`），TTS 调用 `markdownToPlainText`
- `frontend/src/pages/StudyNotes.tsx` — 精简为纯列表页，卡片点击 `navigate('/notes/'+id)`，删除编辑器残留代码
- `frontend/src/pages/Home.tsx` — 核心重写：专辑模式 `Promise.all` 混排媒体+学习页面按时间排序；首页顶部「最近学习页面」区块（最多6个+查看全部）；媒体卡片精简为文件名+专辑/子专辑Tag+紫标签；专辑详情页新建学习页面入口；`location.key` 触发返回刷新
- `frontend/src/pages/Albums.tsx` — Col 断点同步
- `docs/ChangeLog.md` — 新增 [Unreleased] 段

### 关键决策
- 专辑详情页：学习页面与音视频**混排**按更新时间排序（用户选定）
- 首页：额外展示「最近学习页面」区块（用户选定）
- Col 断点统一 `xs=24 sm=12 md=8 lg=6 xl=6 xxl=4`（桌面4个/超宽6个）
- 学习页面卡片预览：第一张图片，无图用 ReadOutlined 橙色渐变占位
- 混排 key 加前缀 `m-`/`n-` 防数字 id 冲突
- NoteEditor 提取为独立路由 `/notes/:id`，支持 URL 直接访问与浏览器后退
- 代码块整体移除（TTS 念代码无意义），图片保留 alt，链接保留文本

### 验证
- `tsc -b` 零错误
- `vite build` 成功（3.43s）
- `go build ./...` + `go vet ./...` 通过（后端未受影响）

### 注意事项（bash 工具）
- 该环境 bash 工作目录默认在项目根 `D:\Code\Go\EchoSub`，且**不在命令间持久化**，每次执行必须在 command 字符串里显式写 `cd /d/Code/Go/EchoSub/frontend && ...`
- pnpm 有 `verify-deps-before-run` 依赖检查会干扰，直接用 `node node_modules/typescript/bin/tsc -b` 和 `node node_modules/vite/bin/vite.js build` 绕过
